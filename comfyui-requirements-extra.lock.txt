# 網頁 GUI + 所有選用功能（ControlNet/ADetailer/mediapipe）套件凍結版本清單。
# 涵蓋 comfyui-requirements.lock.txt（ComfyUI 本體）之外、gui.py 網頁上會用到
# 的一切。分類依據是 `uv pip tree --python ComfyUI\.venv\Scripts\python.exe`
# 對這個 venv 實際跑出來的相依關係，不是憑印象猜的——版本號也是同一個指令
# freeze 出來、目前實測跑得動的版本。已經在 comfyui-requirements.lock.txt
# 裡的套件（huggingface-hub / filelock / pyyaml / scipy / requests / numpy /
# pillow / networkx / psutil / typer / pydantic 等）不重複列在這裡。
#
# 安裝：
#   uv pip install --python .venv\Scripts\python.exe -r comfyui-requirements-extra.lock.txt

# ============================================================
# 網頁 GUI（gui.py）— Gradio
# ============================================================
gradio==6.24.0
gradio-client==2.6.0
hf-gradio==0.4.1
groovy==0.1.2
orjson==3.12.0
pandas==3.0.5
tzdata==2026.3
pydub==0.25.1
python-multipart==0.0.32
pytz==2026.3.post1
safehttpx==0.1.7
semantic-version==2.10.0
tomlkit==0.14.0
brotli==1.2.0

# ============================================================
# 網頁 GUI + image_api.py 共用 — ASGI web server
# （gradio 內建用它跑網頁；image_api.py 直接 import 來對外提供 HTTP API）
# ============================================================
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.3

# ============================================================
# 骨架姿勢控制 ControlNet（comfyui_controlnet_aux 的 OpenPose 前處理器）
# ============================================================
importlib-metadata==9.0.0
zipp==4.1.0
opencv-python==5.0.0.93
scikit-image==0.26.0
imageio==2.37.4
lazy-loader==0.5
tifffile==2026.3.3
matplotlib==3.11.1
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
onnxruntime-gpu==1.28.0
flatbuffers==25.12.19
protobuf==7.35.1

# ============================================================
# 臉部/手部精修 ADetailer（Impact-Pack / Impact-Subpack + ultralytics YOLO）
# ============================================================
segment-anything==1.0
piexif==1.1.3
dill==0.4.1
ultralytics==8.4.120
ultralytics-platform==0.1.5
ultralytics-thop==2.1.6
nvidia-ml-py==13.610.43
polars==1.43.2
polars-runtime-32==1.43.2
# opencv-python、matplotlib 也是 ultralytics 的相依，但已經在上面 ControlNet
# 區塊裝過，這裡不重複列

# ============================================================
# ADetailer 的 MediaPipe 臉部偵測後端（facedetailer_backend="mediapipe"）
# ============================================================
mediapipe==1.0.1
opencv-contrib-python==5.0.0.93
absl-py==2.5.0
sounddevice==0.5.5
cffi==2.1.1
pycparser==3.0
# matplotlib 也是 mediapipe 的相依，已經在上面 ControlNet 區塊裝過

# ============================================================
# InsightFace / ONNX 執行期需要的套件
# insightface 本身跟 onnx 要用 --no-deps 另外裝（見 README「3b. 安裝
# InsightFace」——避免拉到跟 onnxruntime-gpu 衝突的 CPU 版 onnxruntime），
# 這裡只補 --no-deps 不會自動帶進來、但 onnx 實際運作需要的套件
# ============================================================
ml-dtypes==0.6.0
